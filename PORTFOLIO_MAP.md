# Portfolio Map

This historical integration workspace is one part of the RF inductor project,
not the current code source of truth.

The cross-project index, relationship diagram, and generated result catalogs
are located at:

```text
C:\Users\mechrevo\Desktop\rf_inductor_project_hub
```

Use that directory to answer these questions before starting new work:

- Which repository owns reusable single-inductor code?
- Which directory is a release snapshot with reference artifacts?
- Which historical EMX, cascade, balun, and HFSS results exist?

This workspace remains the archive and integration context for LVBOBALUN,
filter/balun, Cadence/EMX, and HFSS experiments. Its nested
`repo_lvbobalun/` checkout is a separate Git repository and should continue to
be managed from inside that directory.
